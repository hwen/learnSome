import tool from './utils';
import {
  logMousePos,
  openNotePad,
  randomMove,
  getPosition,
  searchPixel
} from './utils/auto';

// openNotePad();
// logMousePos();
// randomMove();
// getPosition();
searchPixel();
